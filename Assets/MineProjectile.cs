using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MineProjectile : Projectile
{
    //GameObject explosionPrefab;
    //float explosionLifeTime;
    //Vector3 explosionSize;

    //float explosionDamage;

    //public void InitMine(float _damage, float _velocity, float _lifetime, string _enemyTag, GameObject aoePrefab, float aoeLifetime, Vector3 aoeSize)
    //{
    //    Init(_damage, _velocity, _lifetime, _enemyTag);
    //    explosionDamage = _damage;
    //    damage = 0;
    //    explosionPrefab = _aoePrefab;
    //    explosionSize = _aoeSize;
    //    explosionLifeTime = _aoeLifetime;

    //    weapon = _weapon;
    //}

    //public void Explode(Collider col)
    //{
    //    if (explosionTimer > 0) return;

    //    explosionTimer = explosionCooldown;

    //    AudioManager.Play(explosionAudio, true);

    //    weapon.CreateAoe(enemyTag, aoePrefab, transform.position, Quaternion.identity, missileDamage, aoeLifetime, aoeSize);
    //}
}
